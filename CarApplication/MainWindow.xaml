<Window x:Class="CarApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CarApplication"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="350" Closing="Window_Closing">
    <Grid>

        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File">
                    <MenuItem Header="Export To CSV " />
                    <Separator />
                    <MenuItem Header="_Exit" />
                </MenuItem>
                <MenuItem Header="Add" Click="MenuItemAdd_Click" />
                <MenuItem Header="Delete" />
            </Menu>
            <TextBox AcceptsReturn="True" />
        </DockPanel>

        <ListView Name="lvCars" HorizontalAlignment="Left" Width="350" Margin="0,0,0,0"  Height="330" SelectionChanged="lvCars_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Make Model" Width="100" DisplayMemberBinding="{Binding Make}"></GridViewColumn>
                    <GridViewColumn Header="Engine" Width="100" DisplayMemberBinding="{Binding Engine}"></GridViewColumn>
                    <GridViewColumn Header="Fuel Type" Width="100" DisplayMemberBinding="{Binding Fuel}"></GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
